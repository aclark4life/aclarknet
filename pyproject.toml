[project]
name = "aclarknet"
version = "0.1.0"
dependencies = [
    "dj-database-url",
    "django-debug-toolbar",
    "django-extensions",
    "wagtail",
    "python-webpack-boilerplate",
]

[tool.setuptools]
packages = []

[tool.django_mongodb_cli]
dev = [
    "django-mongodb-template @ git+ssh://git@github.com/aclark4life/django-mongodb-template@main",
]
