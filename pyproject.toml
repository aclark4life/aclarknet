[project]
name = "aclarknet"
version = "0.1.0"
dependencies = [
    "dj-database-url",
    "django-debug-toolbar",
    "django-extensions",
    "wagtail",
    "python-webpack-boilerplate",
]

[tool.setuptools]
packages = []
